/** @type {import('next').NextConfig} */
const nextConfig = {
  //   build: {
  //     extend(config, {}) {
  //       config.node = {
  //         fs: "empty",
  //       };
  //     },
  //   },
};

export default nextConfig;
